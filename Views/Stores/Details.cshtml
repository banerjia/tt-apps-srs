@model tt_apps_srs.Controllers.Store_DetailModel
@using System.Globalization
@{
    TextInfo textInfo = new CultureInfo("en-US", false).TextInfo;
}
<div class="row" style="margin-bottom:1em">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <a class="btn btn-primary btn-md" asp-action="Edit" asp-route-id="@Model.Id">
            <i class="fas fa-pencil-alt"></i>
            <span class="hidden-sm hidden-lg">Edit</span>
        </a>
        <a class="btn btn-primary btn-md" href="#">
            <i class="fas fa-cart-plus"></i>
            <span class="hidden-sm hidden-lg">Create an Order</span>
        </a>
    </div>
</div>
<div class="row">
    <div class="col-lg-6 col-md-6 col-sm-12 col-xs-12">
        <h2 class="h2">Store Information</h2>
        <div class="row">
            <div class="col-12"><label>Status</label></div>
            <div class="col-12">
                @if(Model.Active)
                {
                    <i class="fas fa-circle text-success"></i> 
                    <span class='text-success'>Active</span>
                }
                else
                {
                    <i class="fas fa-circle text-warning"></i> 
                    <span class='text-warning'>Inactive</span>
                }
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <label>Retailer</label>
            </div>
            <div class="col-12">@textInfo.ToTitleCase(Model.Retailer.Name.ToLower())</div>
        </div>
        <div class="row">
            <div class="col-12"><label>Location Number</label></div>
            <div class="col-12">@Model.LocationNumber</div>
        </div>
        <div class="row">
            <div class="col-12"><label>Address</label></div>
            <div class="col-12">
                <address>
                    @textInfo.ToTitleCase(Model.Address.ToLower())
                </address>
            </div>
        </div>
        @if (!String.IsNullOrEmpty(Model.Phone))
        {
            <div class="row">
                <div class="col-12"><label>Phone</label></div>
                <div class="col-12"><tel>@Model.Phone</tel></div>
            </div>

        }
        <h2 class="h2">Order Specifications</h2>
        <p>Specify the parameters and/or criteria to considered while ordering for this store</p>
        <div class="row">
            <div class="col-12"> 
                <label>Maximum total amount for a given order</label>
            </div>
            <div class="col-12">
                @if (Model.MaxOrderAmount != null)
                {
                    @String.Format("{0:c", Model.MaxOrderAmount);
                }
                else
                {
                    <text>No maximum amount specified</text>
                }
            </div>
        </div>
    </div>
    <div class="col-lg-6 col-md-6 col-sm-12 col-xs-12">
        <div class="panel panel-default">   
            <div class="panel-body">
                <div id="gmap_StoreLocation"></div>
            </div>
        </div>
    </div>
</div>
